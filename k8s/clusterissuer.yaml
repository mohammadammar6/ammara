apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    # Real Let's Encrypt endpoint
    server: https://acme-v02.api.letsencrypt.org/directory
    # Use your real email (for expiry notices etc.)
    email: mohdammar.jamil@gmail.com
    privateKeySecretRef:
      name: letsencrypt-prod-account-key
    solvers:
      - http01:
          ingress:
            class: nginx
